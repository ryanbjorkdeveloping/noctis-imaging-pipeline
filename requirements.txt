# Main environment (.venv) — Python 3.11
#
#   python3.11 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# THEN the two packages that are NOT installable from PyPI (see README > Install):
#   .venv/bin/pip install --no-build-isolation git+https://github.com/toros-astro/ois
#   .venv/bin/pip install git+https://github.com/dguevel/PyZOGY
#
# Pins are frozen from a working environment. Two are load-bearing, not cosmetic:
#   photutils 3.0  — renamed args/columns vs 2.x (n_pixels, x_centroid, ...)
#   ztfquery 1.28  — calls the NumPy-2-removed np.in1d; config.py shims it

# --- core numerics ---
numpy==2.4.6
scipy==1.17.1
pandas==3.0.4

# --- astronomy ---
astropy==8.0.0
astropy-iers-data==0.2026.6.22.1.23.34
astropy_healpix==1.1.3
photutils==3.0.0
reproject==0.21.0
sep==1.4.1
ephem==4.2.1
ztfquery==1.28.0
astroquery==0.4.11

# --- Stage-4 inference (braai real/bogus runs here) ---
tensorflow==2.21.0
keras==3.15.0
h5py==3.14.0
scikit-learn==1.9.0
scikit-image==0.26.0

# --- brokers / alert formats ---
alerce==2.3.0
fastavro==1.12.2
cramjam==2.11.0

# --- imaging + plotting ---
opencv-python==4.13.0.92
pillow==12.2.0
matplotlib==3.11.0

# --- misc ---
python-dotenv==1.2.2
statsmodels==0.14.6
tqdm==4.68.4
